# Required: Claude Code authentication (from `claude auth` or Anthropic console)
CLAUDE_CODE_OAUTH_TOKEN=

# Required: GitHub token (for git push + gh pr create inside sandbox)
# Generate at https://github.com/settings/tokens with repo scope
GH_TOKEN=
